# ddflow Repository Instructions

## 本私有工作分支的限制

仓库代码与文档按用户授权采用 MIT 许可证；业务数据和数据处理成果不属于许可证授权范围。未经用户另行明确授权，不推送远程、不合入公开分支或发布软件包。

真实数据处理的测试与运行均在用户指定的数据目录内进行；原始数据、结果、中间文件和含数据的日志只保留在该目录，不写入项目仓库或外部服务。仓库中不保留具体数据任务的专用代码或实验记录。

## 目标

帮助用户以明确任务契约、真实模型分工和可验证输出完成日常长工作流。优先数据清洗；不把产品重新写成专属编码框架。

## 实现之前

先读 [项目入口](README.md)、[产品文档](docs/product.md)、[架构文档](docs/architecture.md)与[使用说明](docs/usage.md)。协议字段以 skills/protocol/workflow-schema.md 为准；执行行为以 skills/protocol/execution-protocol.md 为准。区分已确认边界、本轮建议、已实现能力与未执行的验证，不把文档版本写成软件发行状态。

新增功能前检查是否已有可复用的库、宿主原生能力、Skill 或参考实现。只借用与目标直接相关的部分，核对具体文件许可，不整套引入重框架。

## 必须保留

用户显式调用 Planner / Executor，不自动互相切换。规划不执行业务。工作流为静态 DAG，节点只有一种 Agent Task。Markdown 是唯一工作流状态；depends_on 是唯一机器依赖；节点文件即集合。

四种持久状态，blocked 仅派生，没有 skipped。主 Harness 默认唯一节点文档写入者。产物必须验收后才 completed。任何实际节点失败后不启动新节点，包括独立分支；已启动任务真实收敛，不能伪造取消。failed 不自动重置，继续需要显式 Re-plan 与新节点。

Planner 发现 running 时只读；只直接编辑 pending Contract；不重编号历史 ID，不覆盖 completed / failed。未知 running owner 不抢占。每波重读、重校验，不缓存过期 ready。

显式 model 不得静默替换；model_used 只能记录宿主实际证据，未知不是默认值。必需 Skill 必须真正进入 worker 上下文。原始数据只读，任务 worker 不放宽固定规则与验收器。

worker 写入范围首先是行为约定，不要求所有宿主提供文件系统硬隔离。用户已接受当前 Codex 宿主以软约束开展低风险功能试验；只缺少硬隔离或实际模型标识，不作为这类试验的阻塞条件。主 Harness 仍需验收产物，可按风险比较输入、规则和节点 Contract；发现实际越界或模型不匹配时按执行协议停止。实际模型证据不足时如实记录未知，不为补齐证据自研运行时。

## 不新增

不自研 Runtime、Scheduler、Model Router、Provider / Secret 管理、统一 Adapter、数据库状态中心、动态 DAG、自动模型升级、预算平台、图形编辑器或后台 daemon。不要用几十个新配置字段解决能通过明确文档或宿主原生能力解决的问题。

CLI inspect / Viewer 全部只读；不暗加 run / next / done 或工作流写 API。Viewer 不因文档错误丢弃 Last-Known-Good，不把断连与非法文档混为一谈。

## 实验材料与仓库职责

本仓库交付 Skill、协议、可选只读工具、教学蓝图及维护这些内容所需的最小测试。具体实验的数据、专用业务代码、运行后的节点事实、产物、日志和详细实验报告放在用户指定的仓库外目录，合成实验也适用；不因为创建私有分支就将实验材料入库。仓库仅保留简短的能力验证结论与已知限制，不维护实验流水账。不为仓库外的单次实验、节点或检查创建 Git 空检查点。

## 控制规模

保持单 Package；不提前 Monorepo。节点保持最小充分粒度，不为每条数据创建 Agent。不复制完整大表 / 全部会话到每个 worker。Skill 入口短小，按需加载 references；协议仅在 skills/protocol 维护，打包时生成分发副本。

只运行与修改相关的最少文档 / 单元 / 集成检查。不扩大测试平台，不为了覆盖率加入跨供应商模拟系统。没有运行的 Gate 明确写未运行。

## 可信度

任务事实、假设与未知分开，用户订阅 / API Key / 敏感数据不进入仓库或日志。

提交前检查名称和字段一致、链接与样例依赖有效、双 Skill 包可以脱离源码仓库安装，CLI 可选。任何破坏既有边界的修改，先说明最小必要理由并和用户讨论，不偷偷扩展产品。

## 按里程碑 Git 提交

用户所说的“每一步”指每个里程碑（如 M0、M1、M2）。每完成一个里程碑，完成该里程碑相关验收后统一创建一次 Git commit，不将多个已完成里程碑积压到最后提交。里程碑内部的检查、实验、节点执行、修复和文档调整不逐项提交，也不创建空检查点；除非用户另行明确要求，不因一次对话结束而提前提交未完成的里程碑。

提交信息说明该里程碑的实际交付；只提交相关文件，不夹带用户或其他任务的改动。里程碑完成时汇报提交哈希与验收结果，未通过或未运行的验证如实记录，不以提交代替验收。若提交被权限或工具错误阻止，明确报告原因并保留改动，不能声称已提交。用户要求提交不等于授权推送远程仓库。
